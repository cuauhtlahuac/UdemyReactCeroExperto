// Arreglos en JS
const  arregloEmptySpaces = new Array( 100 ); // Arreglo sólo para crear espacios vacios

const arreglo = [1,2,3,4];
arreglo.push(5);

let arreglo2 = arreglo;
arreglo2.push(6)

console.log( {arreglo} );
console.log( {arreglo2} );